# Porth-IO: Sovereign Bazel Build
load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "porth-core",
    hdrs = glob(["include/porth/*.hpp"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    copts = ["-std=c++23"],
)